<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travel.dao.UserDao">
    <!-- 登录 -->
    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultType="com.example.travel.entity.User">
        SELECT * FROM users WHERE username = #{username} LIMIT 1
    </select>
    <!--  根据id查询  -->
    <select id="findById" resultType="com.example.travel.entity.User">
        SELECT * FROM users WHERE id = #{id}
    </select>
    <!-- 分页查询 -->
    <select id="findAllUsers" resultType="com.example.travel.entity.User">
        SELECT * FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (sku LIKE CONCAT('%', #{keyword}, '%') OR id = #{keyword})
            </if>
        </where>
        LIMIT #{offset},#{pageSize}
    </select>
    <!-- 查询总数（用于分页） -->
    <select id="countUser" resultType="int">
        select COUNT(*) from users
        <where>
            <if test="keyword != null and keyword != ''">
                AND (sku LIKE CONCAT('%', #{keyword}, '%') OR id = #{keyword})
            </if>
        </where>
    </select>
    <select id="userimage" resultType="com.example.travel.entity.User">
        select id,username,image from users where uesername = #{username}
    </select>
    <!-- 删除 -->
    <delete id="deleteUser" parameterType="java.lang.Long">
        DELETE FROM users WHERE id = #{id}
    </delete>
    <!-- 修改 -->
    <update id="updateUser" parameterType="com.example.travel.entity.User">
        UPDATE users
        SET username = #{username},
            email = #{email},
            password = #{password},
            phone = #{phone},
            nickname = #{nickname},
            image = #{image},
            signature = #{signature},
            travelmage = #{travelmage},
            experience = #{experience}
        WHERE id = #{id}
    </update>
    <!-- 新增 -->
    <insert id="insertUser" parameterType="com.example.travel.entity.User">
        INSERT INTO users
            (image, username, email, password, phone, nickname, createTime)
        VALUES
            (#{image}, #{username}, #{email}, #{password}, #{phone}, #{nickname}, #{createTime})
    </insert>

    <!-- 新增查询方法 -->
    <select id="findByEmail" resultType="com.example.travel.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findByPhone" resultType="com.example.travel.entity.User">
        SELECT * FROM users WHERE phone = #{phone}
    </select>
</mapper>